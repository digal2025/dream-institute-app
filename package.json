{
  "name": "dream-institute-fee-management",
  "version": "1.0.0",
  "description": "Dream Institute Fee Management Application",
  "main": "zoho-invoice-api/index.js",
  "scripts": {
    "start": "cd zoho-invoice-api && npm start",
    "build": "npm install --omit=dev --no-audit --no-fund && cd zoho-invoice-api && npm install --omit=dev --no-audit --no-fund && cd client && npm install --omit=dev --no-audit --no-fund && CI=false DISABLE_ESLINT_PLUGIN=true GENERATE_SOURCEMAP=false SKIP_PREFLIGHT_CHECK=true NODE_OPTIONS='--max-old-space-size=4096' npm run build",
    "dev": "cd zoho-invoice-api && npm run dev",
    "install-all": "npm install && cd zoho-invoice-api && npm install && cd client && npm install",
    "test": "cd zoho-invoice-api && npm test"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "jsonwebtoken": "^9.0.2",
    "react-router-dom": "^6.20.1",
    "uuid": "^11.1.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
