{
  "build": {
    "command": "./build.sh || (echo 'Build script failed, trying fallback...' && npm install --omit=dev && cd zoho-invoice-api && npm install --omit=dev && cd client && npm install --omit=dev && CI=false DISABLE_ESLINT_PLUGIN=true GENERATE_SOURCEMAP=false npm run build)"
  },
  "start": {
    "command": "npm start"
  },
  "environment": {
    "NODE_ENV": "production",
    "PORT": "3000",
    "CI": "false",
    "DISABLE_ESLINT_PLUGIN": "true",
    "GENERATE_SOURCEMAP": "false",
    "NODE_OPTIONS": "--max-old-space-size=4096"
  }
} 